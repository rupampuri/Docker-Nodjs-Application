{
  "name": "docker",
  "version": "1.0.0",
  "description": "nodejs webapp under the docker container",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rupam",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
